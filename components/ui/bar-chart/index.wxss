.bar-chart {
  position: relative;
  width: 100%;
  transform: translateZ(0);
  will-change: transform;
}
.bar-chart__canvas {
  display: block;
  width: 100%;
  height: 100%;
  transform: translateZ(0);
}
